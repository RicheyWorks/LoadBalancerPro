# Operator Install Run Matrix

This matrix gives operators and reviewers one place to choose a release-free command path for CI-built artifacts, local builds, proxy demos, static status pages, and verification helpers. Every row below is release-free: it consumes source, local build output, or GitHub Actions workflow artifacts only. Workflow artifacts are not GitHub Release assets.

For goal-based navigation before choosing a command path, start with [`REVIEWER_TRUST_MAP.md`](REVIEWER_TRUST_MAP.md).

## Matrix

| Use case | Command path | Windows command | Unix command | Inputs required | What it proves | Expected evidence | Release-free? | Notes/limitations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Build package locally | Maven package | `mvn -B -DskipTests package` | `mvn -B -DskipTests package` | JDK and Maven dependency resolution | The Spring Boot jar can be built from the checked-out source | `target/LoadBalancerPro-2.4.2.jar` exists | Yes | Local Maven may be blocked by workstation trust-chain configuration; CI remains the source of truth when local Maven cannot resolve dependencies. |
| Run packaged jar | Java executable jar | `java -jar target/LoadBalancerPro-2.4.2.jar --server.address=127.0.0.1 --server.port=8080` | `java -jar target/LoadBalancerPro-2.4.2.jar --server.address=127.0.0.1 --server.port=8080` | Built jar | The packaged API can start from the jar path | `/api/health` responds when the app is running | Yes | Default proxy mode remains disabled unless a proxy profile or explicit property enables it. |
| Run Maven Spring Boot app | Spring Boot Maven plugin | `mvn spring-boot:run "-Dspring-boot.run.arguments=--server.address=127.0.0.1 --server.port=8080"` | `mvn spring-boot:run "-Dspring-boot.run.arguments=--server.address=127.0.0.1 --server.port=8080"` | Source checkout, JDK, Maven | The app can run from source without packaging first | `/api/health` responds when the app is running | Yes | This path still needs Maven dependency resolution. |
| Run `ProxyDemoFixtureLauncher` via Maven exec | Maven exec launcher | `mvn -q -DskipTests compile exec:java "-Dexec.mainClass=com.richmond423.loadbalancerpro.demo.ProxyDemoFixtureLauncher" "-Dexec.args=--mode round-robin"` | `mvn -q -DskipTests compile exec:java "-Dexec.mainClass=com.richmond423.loadbalancerpro.demo.ProxyDemoFixtureLauncher" "-Dexec.args=--mode round-robin"` | Source checkout, JDK, Maven | The Java loopback fixture launcher starts cross-platform backend fixtures | Printed backend URLs, health URLs, proxy startup command, curl recipes, and status URLs | Yes | No Python, Node, Docker, cloud service, or public network is required for the Java launcher path. |
| Run `ProxyDemoFixtureLauncher` from classes | Compiled classpath launcher | `mvn -q -DskipTests compile; java -cp target/classes com.richmond423.loadbalancerpro.demo.ProxyDemoFixtureLauncher --mode round-robin` | `mvn -q -DskipTests compile && java -cp target/classes com.richmond423.loadbalancerpro.demo.ProxyDemoFixtureLauncher --mode round-robin` | Compiled classes | The fixture launcher can run without a manual jar entry point change | Same launcher output as Maven exec | Yes | Keep this as a fallback; Maven exec is easier for most operators. |
| Start proxy demo stack | Demo helper script | `powershell -ExecutionPolicy Bypass -File scripts/proxy-demo.ps1 -Mode round-robin` | `bash scripts/proxy-demo.sh --mode round-robin` | Source checkout, JDK, Maven, loopback ports | The local two-backend proxy demo path is discoverable | Fixture output, printed app command, curl recipes, `/proxy-status.html` URL | Yes | The scripts print and run loopback-only demo commands. |
| Use `ROUND_ROBIN` demo profile | Spring profile | `java -jar target/LoadBalancerPro-2.4.2.jar --spring.profiles.active=proxy-demo-round-robin` | `java -jar target/LoadBalancerPro-2.4.2.jar --spring.profiles.active=proxy-demo-round-robin` | Built jar and fixture backends | Real HTTP requests can alternate across configured loopback upstreams | `X-LoadBalancerPro-Upstream`, `X-LoadBalancerPro-Strategy`, and status counters | Yes | Proxy mode is enabled only by the explicit demo profile. |
| Use `WEIGHTED_ROUND_ROBIN` demo profile | Spring profile | `java -jar target/LoadBalancerPro-2.4.2.jar --spring.profiles.active=proxy-demo-weighted-round-robin` | `java -jar target/LoadBalancerPro-2.4.2.jar --spring.profiles.active=proxy-demo-weighted-round-robin` | Built jar and fixture backends | Real HTTP requests can exercise weighted proxy selection | Selected upstream headers and `/api/proxy/status` counters | Yes | Treat observed distribution as demo evidence, not a throughput or benchmark claim. |
| Use failover demo profile | Spring profile | `java -jar target/LoadBalancerPro-2.4.2.jar --spring.profiles.active=proxy-demo-failover` | `java -jar target/LoadBalancerPro-2.4.2.jar --spring.profiles.active=proxy-demo-failover` | Built jar and fixture backends | Health-aware proxy selection skips an unhealthy loopback backend | Healthy upstream header evidence, failure counters, and status UI health state | Yes | Health checks and cooldown remain optional and local/process-only. |
| Use real-backend example profiles | Copy/adapt example config | `java -jar target/LoadBalancerPro-2.4.2.jar --spring.config.import=optional:file:docs/examples/proxy/application-proxy-real-backend-round-robin-example.properties` | `java -jar target/LoadBalancerPro-2.4.2.jar --spring.config.import=optional:file:docs/examples/proxy/application-proxy-real-backend-round-robin-example.properties` | Local backend services using loopback/private placeholders | Operators can adapt proxy config beyond fixture demos | Proxy headers, `/proxy-status.html`, and `/api/proxy/status` reflect configured local services | Yes | Example files use loopback/private placeholders only and are not active by default. The legacy `application-proxy-real-backend-example.properties` file remains available. See [`REAL_BACKEND_PROXY_EXAMPLES.md`](REAL_BACKEND_PROXY_EXAMPLES.md). |
| Open proxy status page | Static browser page | `Start-Process http://localhost:8080/proxy-status.html` | `xdg-open http://localhost:8080/proxy-status.html` | Running app | The read-only proxy operator UI is packaged and served | Browser page displays status JSON, upstreams, counters, retry/cooldown state, and demo commands | Yes | The page uses same-origin `GET /api/proxy/status` only and has no reset/write controls. |
| Open load-balancing cockpit | Static browser page | `Start-Process http://localhost:8080/load-balancing-cockpit.html` | `xdg-open http://localhost:8080/load-balancing-cockpit.html` | Running app | The packaged cockpit static page is available | Browser page loads from the running app | Yes | Local/operator review surface only; it does not publish evidence or mutate release state. |
| Inspect proxy status endpoint | Read-only API | `curl -s http://127.0.0.1:8080/api/proxy/status` | `curl -s http://127.0.0.1:8080/api/proxy/status` | Running app | Proxy status source is reachable | JSON includes enabled state, strategy, upstreams, counters, retry/cooldown fields | Yes | Endpoint is read-only. |
| Consume CI packaged smoke artifact | GitHub Actions workflow artifact | Download `packaged-artifact-smoke` from the selected CI run | Download `packaged-artifact-smoke` from the selected CI run | GitHub Actions run | CI inspected the built jar resources and checksum output | `artifact-smoke-summary.txt`, `artifact-sha256.txt`, `jar-resource-list.txt` | Yes | This is workflow artifact evidence, not a GitHub Release asset. |
| Consume JaCoCo artifact | GitHub Actions workflow artifact | Download `jacoco-coverage-report` from the selected CI run | Download `jacoco-coverage-report` from the selected CI run | GitHub Actions run | Coverage report files were generated by CI | HTML/XML JaCoCo report files and CI coverage summary | Yes | Coverage is evidence, not a complete behavior guarantee. |
| Consume SBOM artifact | GitHub Actions workflow artifact | Download `loadbalancerpro-sbom` from the selected CI run | Download `loadbalancerpro-sbom` from the selected CI run | GitHub Actions run | SBOM files were generated by CI | `bom.json` and `bom.xml` | Yes | SBOM files are workflow evidence, not signed release assets or vulnerability-free proof. |
| Run local artifact verification helper | Local helper script | `powershell -ExecutionPolicy Bypass -File scripts/local-artifact-verify.ps1 -JarPath target/LoadBalancerPro-2.4.2.jar` | `bash scripts/local-artifact-verify.sh target/LoadBalancerPro-2.4.2.jar` | Built local jar | Local jar checksum and packaged resource checks are inspectable | SHA-256 output and matching resource entries | Yes | Do not commit generated checksums or jars. |
| Run operator distribution smoke helper | Local helper script | `powershell -ExecutionPolicy Bypass -File scripts/operator-distribution-smoke.ps1` | `bash scripts/operator-distribution-smoke.sh` | Source checkout, optional built jar | The operator smoke path can verify docs, profiles, static resources, and optional local HTTP startup | Printed checks and status URLs | Yes | The helper avoids tag, release, release asset, and `release-downloads/` operations. |
| Try optional JavaFX desktop UI | Interactive CLI GUI option | `mvn -q exec:java "-Dexec.mainClass=com.richmond423.loadbalancerpro.cli.LoadBalancerCLI"` then choose `15. Launch GUI` | `mvn -q exec:java "-Dexec.mainClass=com.richmond423.loadbalancerpro.cli.LoadBalancerCLI"` then choose `15. Launch GUI` | JavaFX-capable runtime and desktop display | The optional desktop simulator UI can be attempted locally | JavaFX window opens, or a platform-specific launch error is documented | Yes | Not required for API, proxy, static browser pages, fixture launcher, CI artifacts, or operator smoke paths. See [`JAVAFX_OPTIONAL_UI.md`](JAVAFX_OPTIONAL_UI.md). |

## Release-Free Boundaries

- Every matrix row is release-free.
- CI artifacts are GitHub Actions workflow artifacts, not GitHub Release assets.
- The matrix does not create tags, GitHub Releases, release assets, or `release-downloads/` files.
- The matrix does not change default application behavior or enable proxy mode by default.
- Proxy rows describe lightweight local/operator behavior only, not a production gateway or certification claim.
- JavaFX is optional desktop UI support and is not required for the API/proxy/operator paths.
- Future release publication requires a separate, explicit user request after completing [`RELEASE_INTENT_CHECKLIST.md`](RELEASE_INTENT_CHECKLIST.md).
